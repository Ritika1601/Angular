import { Component } from '@angular/core';
import {UserForm } from './model.component';

describe('StudentForm', () => {
  it('should create an instance', () => {
    expect(Component).toBeTruthy();
  });
});
